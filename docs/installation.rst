============
Installation
============

At the command line::

    $ easy_install drf-service-token-authenticator

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv drf-service-token-authenticator
    $ pip install drf-service-token-authenticator
